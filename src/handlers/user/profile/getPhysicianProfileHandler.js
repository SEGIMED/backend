const getPhysicianProfileHandler = async ({id, role}) => {
    try {
        
    } catch (error) {
        throw new Error("Ocurrió un error al recuperar los datos del paciente: " + error.message);
        
    }
}
export default getPhysicianProfileHandler