import SegimedAPIError from "../../../error/SegimedAPIError.js";
import models from "../../../databaseConfig.js"

const getOrdersByIdHandlerPhysician = async (userId) => {
    try {
        const registerOrders = await 
    } catch (error) {
        throw new SegimedAPIError("Error al procesar la informacion: ", error)
    }
};
